# Telegram
TELEGRAM_BOT_TOKEN=

# Optional. When unset or unavailable, the bot returns an article excerpt instead.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash

# Optional paths. Relative paths are resolved from the repository root.
IIKO_ASSISTANT_DATABASE=data/iiko_assistant.sqlite3
IIKO_ASSISTANT_CALENDAR=config/production_calendar.json
